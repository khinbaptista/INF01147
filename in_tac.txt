a: byte 1;
b: short 10;
c: float 1.5;
d : byte 1.5;
v : long[5];

long soma(long op1, long op2)
	return op1 + op2;

long f() return 1;
byte main() {
	print "string";
	a = soma(10, 10);

	c = v[3];

	when (1 > a) then ;

	for (a = 1 to 5) {
		print a;
	};

	while (a >= 10)
		a = a - 1;
	return 0;
};
